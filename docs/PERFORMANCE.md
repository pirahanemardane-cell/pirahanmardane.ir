# Performance — وضعیت بسته‌شده پایه

## انجام‌شده
- ایندکس DB + trgm
- rate-limit نقاط حساس
- catalog-cache helper + invalidate بعد از ذخیره محصول
- تصویر WebP در آپلود
- CF Bypass API/panels + DYNAMIC تأیید شد
- vercel headers استاتیک

## عمداً بعداً
- صف، replica، worker، search engine

## مانیتور دستی
- Vercel Speed Insights
- LCP روی موبایل صفحه اصلی و PLP بعد از تغییر بزرگ UI
